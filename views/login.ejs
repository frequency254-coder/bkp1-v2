<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Frequency ENT</title>

    <!-- 🎨 Styles -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        body {
            background: radial-gradient(circle at top, #0a0a0a, #000 60%);
            color: #f8f9fa;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        main {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .login-card {
            background: rgba(10, 10, 20, 0.9);
            border: 1px solid rgba(0, 255, 255, 0.1);
            box-shadow: 0 0 25px rgba(0, 255, 255, 0.15), 0 0 60px rgba(255, 0, 255, 0.05);
            border-radius: 16px;
            padding: 2rem;
            width: 100%;
            max-width: 420px;
            transition: all 0.3s ease;
        }

        .login-card:hover {
            transform: scale(1.02);
            box-shadow: 0 0 40px rgba(0, 255, 255, 0.25);
        }

        h3 {
            text-align: center;
            color: #00ffff;
            text-shadow: 0 0 10px #00ffff, 0 0 30px #ff00ff;
            font-weight: 600;
        }

        .form-control {
            background-color: rgba(0, 0, 0, 0.6);
            color: #fff;
            border: 1px solid rgba(0, 255, 255, 0.3);
            border-radius: 10px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        .form-control:focus {
            border-color: #00ffff;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.4);
        }

        .btn-neon {
            background: linear-gradient(90deg, #00ffff, #ff00ff);
            border: none;
            color: #fff;
            font-weight: 600;
            border-radius: 10px;
            transition: all 0.3s ease;
            box-shadow: 0 0 15px rgba(0, 255, 255, 0.4);
        }

        .btn-neon:hover {
            box-shadow: 0 0 30px rgba(0, 255, 255, 0.8);
            transform: translateY(-2px);
        }

        .text-muted a {
            color: #00ffff;
            text-decoration: none;
        }

        .text-muted a:hover {
            text-decoration: underline;
        }

        .alert {
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            border: 1px solid rgba(255, 0, 0, 0.4);
        }

        @keyframes flicker {
            0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% {
                opacity: 1;
            }
            20%, 24%, 55% {
                opacity: 0.4;
            }
        }

        .glow-logo {
            font-size: 2.5rem;
            color: #00ffff;
            text-shadow: 0 0 20px #00ffff, 0 0 40px #ff00ff;
            animation: flicker 2s infinite;
        }
    </style>
</head>
<body>

<!-- 🔝 Navbar -->
<%- include('partials/_header') %>

<!-- 💫 Main Content -->
<main>
    <div class="login-card shadow-lg">
        <div class="text-center mb-4">
            <i class="bi bi-person-circle glow-logo"></i>
        </div>

        <h3 class="mb-4">Welcome Back</h3>

        <div id="alertContainer"></div>

        <form id="loginForm">
            <div class="mb-3">
                <label for="email" class="form-label text-light">Email</label>
                <input
                        type="email"
                        class="form-control"
                        id="email"
                        name="email"
                        placeholder="Enter your email"
                        autocomplete="username"
                        required>
            </div>

            <div class="mb-3">
                <label for="password" class="form-label text-light">Password</label>
                <input
                        type="password"
                        class="form-control"
                        id="password"
                        name="password"
                        placeholder="Enter your password"
                        autocomplete="current-password"
                        required>
            </div>

            <button type="submit" id="loginBtn" class="btn btn-neon w-100 py-2">
                <i class="bi bi-box-arrow-in-right me-2"></i> Login
            </button>
        </form>

        <div class="text-center text-muted mt-4">
            Don’t have an account? <a href="/register">Register</a>
        </div>
    </div>
</main>

<!-- 🔻 Footer -->
<%- include('partials/_footer') %>

<!-- ⚙️ Scripts -->
<script src="/js/bootstrap.bundle.min.js"></script>
<script src="/js/login.js" defer></script>
</body>
</html>
